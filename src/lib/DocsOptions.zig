//! Per-run options for documentation rules, loaded from project config.

/// When true, `missing_doc_comment` also requires `///` on each named function parameter.
require_function_param_docs: bool = false,
