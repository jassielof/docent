= Test Suite

The test suite verifies the correctness of linting rules and doubles as living examples of what Docent considers valid and invalid.

Each Zig test module is documented with its purpose and the rules it covers. To browse valid and invalid cases directly, refer to the link:fixtures[fixtures directory].
