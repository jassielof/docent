// Considering this is the entry point of the Docent module, in `src/lib/root.zig`.
//! Module docent provides code quality checks, such as documentation, style and complexity. As well as a module scanner that uses the standard library's AST module.
